public calss DemoContinueLoopLabel {

    public static void main(Sting[] args) {
        int a, b;
        Mulai:
        for (a=0;a<2;a++)
            for(b=0;b<3;b++) {
                if (b==)
                    continue
                            Mulai;
                System.out.println("a=" + a + " ; b=" +b);
            
            }
    }
}